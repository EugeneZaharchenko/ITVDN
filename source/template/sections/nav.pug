nav.nav
    - var items = ["home", "о нас", "технологии", "наши работы"]
    each item in items
        if item  === "Н"
            a.nav__link--active(href="#")= item
        else
            a.nav__link(href="#")= item